{% for message in app.session.flashBag.get('danger') %}
    <div class="alert alert-danger no-border">
        <button type="button" class="close" data-dismiss="alert"><span>×</span><span class="sr-only">{{ 'Close' | trans }}</span></button>
        <span class="text-semibold">Error!</span> {{ message }}
    </div>
{% endfor %}