<div class="card">
    <div class="card-header">
        <i class="fa fa-align-justify"></i> News Manager
    </div>
    <div class="card-block">
        <table class="table">
            <thead>
            <tr>
                <th>#</th>
                <th>Period</th>
                <th>News Title</th>
                <th>Owner</th>
                <th>Report Description</th>
                <th>Feedback</th>
                <th>Status</th>
                <th>Created by</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {%  for entity in entities %}
                <tr>
                    <td>{{ entity.id }}</td>
                    <td>{{ entity.created | ago }}</td>
                    <td>
                        {% if entity.news %}
                            {{ entity.news.title }}
                        {% endif %}
                    </td>
                    <td>
                        {% if entity.news.createdBy %}
                            {{ entity.news.createdBy.firstName }}
                        {% endif %}
                    </td>
                    <td>{{ entity.description | truncate(20) }}</td>
                    <td>{{ entity.feedback }}</td>
                    <td>
                        {% if entity.status %}
                            {{ entity.status.name }}
                        {% endif %}
                    </td>
                    <td>
                        {% if entity.createdBy %}
                            {{ entity.createdBy.username }}
                        {% endif %}
                    </td>
                    <td>
                        <a href="{{ path('news_report_edit', {id: entity.id}) }}" data-toggle="tooltip" title="view"><i class="fa fa-eye"></i></a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        <ul class="pagination">
            <li class="page-item"><a class="page-link" href="#">Prev</a>
            </li>
            <li class="page-item active">
                <a class="page-link" href="#">1</a>
            </li>
            <li class="page-item"><a class="page-link" href="#">2</a>
            </li>
            <li class="page-item"><a class="page-link" href="#">3</a>
            </li>
            <li class="page-item"><a class="page-link" href="#">4</a>
            </li>
            <li class="page-item"><a class="page-link" href="#">Next</a>
            </li>
        </ul>
    </div>
</div>